FROM node:18
WORKDIR /app

# Install backend dependencies
# This Dockerfile is intended to be built with context: backend/node-server
COPY package*.json ./
RUN npm ci && npm install resend dotenv @sendgrid/mail express-rate-limit

# Copy backend source
COPY . ./

EXPOSE 3000
CMD ["node", "index.js"]
