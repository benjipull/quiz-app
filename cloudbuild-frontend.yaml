# cloudbuild-frontend.yaml
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/frontend-react:$COMMIT_SHA', '.']
    dir: 'frontend-react'

images:
  - 'gcr.io/$PROJECT_ID/frontend-react:$COMMIT_SHA'
